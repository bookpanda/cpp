# cpp


```bash
# gcc is just clang++ in disguise
# all same output: Apple clang version 15.0.0
gcc --version
g++ --version
clang++ --version


gcc main.cpp

clang++ -std=c++17 struct.cpp -o struct
```